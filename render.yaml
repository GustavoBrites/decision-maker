services:
  - type: web
    name: decision-maker
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: decision-maker-db
          property: connectionString
      - key: PORT
        value: 10000

databases:
  - name: decision-maker-db
    plan: free
